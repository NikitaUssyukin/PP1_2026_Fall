#include <iostream>

using namespace std;

int main() {

    int x;
    cin >> x;

    // determine if it is more than 10 and less than 100
    if(x > 10 && x < 100) {
        cout << "correct" << endl;
    }

    return 0;
}