#include <iostream>
#include <cmath>

using namespace std;

int main() {
    
    double num;
    cin >> num;

    cout << round(num) << endl;
    cout << ceil(num) << endl;
    cout << floor(num) << endl;

    return 0;
}